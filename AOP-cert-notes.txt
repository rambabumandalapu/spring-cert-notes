AOP questions 
=======
1. Which statements are correct, considering the following statements regarding the JoinPoint argument in Spring AOP: (select 3)

We can have only one Joinpoint argument. 
It is supported by all the advice types in Spring AOP. [wrong] because it not supported by arounf advice.
It can be used to retrieve the arguments of the join point (method arguments).
It must be the first parameter of an advice.


2. What term best corresponds to the following description?

"It's a point during the execution of a program. It's always a method execution in AOP."
Ans- JoinPoint

3. Consider the following code snippet:
@RunWith(SpringRunner.class)
@ContextConfiguration
@Transactional
public class MyTest {
    @Test
    public void testA(){
                //dummy content
    }
 
    @Test
    public void testB(){
        //dummy content
   }
}
Which of the following statements are correct? Please select all that apply. (select 3)
@Transactional can be placed on top of testA() instead of MyTest and the class logic will be the same. [Wrong]
Both testA() and testB() will be executed in their own separate transaction. 
Transactions for all the tests that are executed in the MyTest class will be rolled back.
We can add @Commit on top of MyTest class to commit the transaction after each test execution.

4.What is true about Proxying Mechanisms? 
If the target object does not implement any interfaces, a CGLIB proxy is created.
If the target object to be proxied implements at least one interface, a JDK dynamic proxy is used.









